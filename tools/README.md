# Tools

Command‑line tools for working with OCRC.

- `roi-generator/` – generate ROIs.
- `hdf-validator/` – validate HDF JSON files.
- `rocrate-validator/` – check RO-Crate compliance.

Each tool has its own README and requirements.
