# GraphQL API Specification

OCRC defines a unified GraphQL API for querying research objects across repositories.

- `schema.graphql` – the GraphQL schema.
- `examples/queries/` – sample queries.

Implementations may expose this API to allow cross‑repository searches.
