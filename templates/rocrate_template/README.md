# RO-Crate Template

Copy this directory and replace `{{VARIABLES}}` in `ro-crate-metadata.json.template` with actual values. Place your data files in `data/`.

Then rename the template file to `ro-crate-metadata.json`.
